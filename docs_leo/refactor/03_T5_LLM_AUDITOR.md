# T5: llm-auditor - Question en código

Se reemplazaron las instrucciones del `question` tool en `llm-auditor.txt` por
un formato JSON estructurado. El LLM ahora devuelve scores en JSON en lugar de
llamar al question tool. El procesamiento de preguntas TUI se hará por código
(próxima iteración).
